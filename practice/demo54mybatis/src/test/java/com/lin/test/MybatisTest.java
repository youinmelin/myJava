package com.lin.test;import com.lin.dao.IUserDao;import com.lin.domain.User;import org.apache.ibatis.io.Resources;import org.apache.ibatis.session.SqlSession;import org.apache.ibatis.session.SqlSessionFactory;import org.apache.ibatis.session.SqlSessionFactoryBuilder;import java.io.IOException;import java.io.InputStream;import java.util.List;public class MybatisTest {    public static void main(String[] args) throws IOException {        //1. import setting file        InputStream is = Resources.getResourceAsStream("SqlMapConfig.xml");        int ret = is.read();        System.out.println(ret);        //2. build SqlSessionFactory        SqlSessionFactoryBuilder builder = new SqlSessionFactoryBuilder();        SqlSessionFactory factory = builder.build(is);        //3. use factory to create SqlSession object        SqlSession session = factory.openSession();        //4. use SqlSession to create Dao interface's proxy object        IUserDao userDao = session.getMapper(IUserDao.class);        //5. use proxy object implement method        List<User> users = userDao.findAll();        System.out.println(users);        //6. release resource        session.close();        is.close();    }}